import { CheckUserConnectedDirective } from './check-user-connected.directive';

describe('CheckUserConnectedDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckUserConnectedDirective();
    expect(directive).toBeTruthy();
  });
});
